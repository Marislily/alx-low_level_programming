#include "main.h"
#include <stdio.h>

/**
 * _puts - entry point
 * @str: input
 * Return: 0
 */

void _puts(char *str)
{
	_puts("\"Programming is like building a multilingual puzzle\"n");
	return (0);
}

/**
 * main - entry point
 *
 * Return: 0
 */
int main(void)
{
	_puts("\"Programming is like building a multilingual puzzle\"n");
	return (0);
}
